# 二维海气平台交付状态

本文件区分已实现功能与实际运行证据。最终发布需要完整24小时矩阵及两组30天试验通过；启动长实验不等于通过。

## 已完成的实现与验证

- 模型/单位/正号/边界/表面代理/限制说明：[平台指南](air_sea_research_platform.md)。基线配置与历史源码摘要冻结于 `experiments/air_sea/baseline/`。
- 18过程、22项阶段积分预算，诊断海洋水库、外部恢复源汇、约束反力和耗散分项。
- 独立JAX COARE3.5核心，固定NOAA提交与SHA256；10m默认代理、5/20m敏感性、关闭稳定度的机制对照。
- JSON运行配置、NetCDF逐帧写出、原子NPZ+JSON检查点、校验和、现场保留、SIGINT/SIGTERM暂停与精确续跑。
- 非整点暂停不增加诊断记录；回退检查点更新最新指针；批量验收收到中断即停止。
- JAX专用CI定义，自动24h空间/时间/积分矩阵、敏感性和30d验收入口。
- 报告/边界剖面/节点RH/能谱/误差分离图；NetCDF和历史NPZ兼容动画。
- 本地完整短测试：**52通过，125.43秒**。涵盖官方参考、MRI阶条件与光滑四阶、过程预算、动能缺陷收敛、保存现场、校验和、输出对齐、暂停/回退/信号中断。
- wheel已构建；独立虚拟环境（复用已安装依赖）已在仓库外完成 COARE 1 小时模型冒烟验证。依赖锁定文件随配置提供。

## 已获得的数值证据

旧冻结平台v1常系数 n=33/49/65/81、H150的四组24h运行完成；预算均通过。n33与历史24h系数对比在浮点舍入量级。动能预算缺陷在H2400/1200/600/300上约四阶减小。弱风和稳定度分段专项报告不同于光滑分支，未将它们宣称为四阶。

v1批处理在工程重启边界修复前已主动中断，证据保存在 `build/air_sea_acceptance_v1/`，不作为最终完整验收。旧wheel与关键源码快照保留。

## 按用户要求停止验收矩阵

- 最终冻结版本：`build/air_sea_acceptance_v2/`。
- 顺序：常系数空间/时间、常系数积分、COARE空间/时间、COARE积分、敏感性、两组30天（含第1天暂停/恢复）。
- 完整结果读取各套件的 `validation.json`；单组读取 `status.json`、`diagnostics.jsonl` 和失败现场。
- 按用户要求已停止矩阵，不再启动长实验。v2 的 n33/H150 已完成24h；n49/H150 在49800秒（第83同步步）安全暂停并保存检查点。批处理随之退出。
- 24h完整矩阵和两组30天验收均未完成，不宣称发布验收通过。30天不代表气候平衡。
